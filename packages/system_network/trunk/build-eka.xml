<?xml version="1.0" encoding="UTF-8"?>

<project name="system.network-SA" default="main" basedir=".">
    
    <target name="define">
        
        <property name="FLEX_HOME_MAC" value="/opensource/adobe/flex_sdk_4.6" />
        <property name="FLEX_HOME_WIN" value="c:/opensource/flex_sdk_4.6" />
        
        <property name="local.flashplayerversion" value="11.1" />
        
        <condition property="FLEX_HOME" value="${FLEX_HOME_MAC}">
            <os family="mac" />
        </condition>
        
        <condition property="FLEX_HOME" value="${FLEX_HOME_WIN}">
            <os family="windows" />
        </condition>
        
        <property file="build/build.properties"/>
        
        <tstamp>
            <format property="TODAY" pattern="dd MMMM yyyy" />
        </tstamp>
        
        <taskdef resource="flexTasks.tasks" classpath="${FLEX_HOME}/ant/lib/flexTasks.jar" />
        
    </target>
    
    <target name="clean" depends="define">
        <delete dir="${basedir}/${release.dir}" />
    </target>
    
    <import file="meta.xml" />
    
</project>