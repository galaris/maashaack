<?xml version="1.0" encoding="UTF-8"?>

<project name="Maashaack maashaack.swc library AS3" default="main">
    <property file="./build.properties"/>
    <target name="main">
        <exec executable="${compc}" >
            <arg line="-load-config+=${config}" />
            <arg line="-compiler.source-path+=${library.core.src}" />
            <arg line="-compiler.source-path+=${library.system.src}" />
            <arg line="-compiler.source-path+=${library.graphics.src}" />
        </exec>
    </target>
</project>
