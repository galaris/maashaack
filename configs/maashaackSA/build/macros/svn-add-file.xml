
<project>
    
    <macrodef name="svn-add-file">
        <attribute name="path"/>
        <attribute name="force" default="false"/>
        
        <sequential>
            <svn username="${credentials.svn.username}" password="${credentials.svn.password}" javahl="false" svnkit="false">
                <add file="@{path}" force="@{force}"/>
            </svn>
        </sequential>
    </macrodef>
    
</project>