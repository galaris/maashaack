<project>
    
    <target name="compile-system_evaluators">
        
        <propertycopy name="path" from="project.fullpath" override="true"/>
        
        <!-- build -->
        <ant antfile="${path}/meta.xml" target="main" inheritAll="false">
            <property name="FLEX_HOME" value="${FLEX_HOME}" />
            <property name="TODAY" value="${TODAY}" />
            <property name="local.flashplayerversion" value="${local.flashplayerversion}" />
            <property file="${path}/build/build.properties"/>
        </ant>
        
        <!-- deploy to libs -->
        <copy file="${path}/bin-release/system.evaluators.swc" todir="libs/swc" overwrite="true" />
        <!--<copy file="${path}/bin-release/system.evaluators.abc" todir="libs/abc" overwrite="true" />-->
        
        <!-- sync with other projects--> 
        <copy file="${path}/bin-release/system.evaluators.swc" todir="packages/system_formatter/lib-swc" overwrite="true" />
        <copy file="${path}/bin-release/system.evaluators.abc" todir="packages/system_formatter/lib-abc" overwrite="true" />
        
    </target>
    
</project>
