<project>
    
    <target name="compile-system_models">
        
        <propertycopy name="path" from="project.fullpath" override="true"/>
        
        <!-- build -->
        <ant antfile="${path}/meta.xml" target="main" inheritAll="false">
            <property name="FLEX_HOME" value="${FLEX_HOME}" />
            <property name="TODAY" value="${TODAY}" />
            <property name="local.flashplayerversion" value="${local.flashplayerversion}" />
            <property file="${path}/build/build.properties"/>
        </ant>
        
        <!-- deploy to libs -->
        <copy file="${path}/bin-release/system.models.swc" todir="libs/swc/packages" overwrite="true" />
        <!--<copy file="${path}/bin-release/system.models.abc" todir="libs/abc/packages" overwrite="true" />-->
        
        <!-- sync with other projects--> 
        
        <copy file="${path}/bin-release/system.models.swc" todir="packages/graphics/lib-swc" overwrite="true" />
        
    </target>
    
</project>
