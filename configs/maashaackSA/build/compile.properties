#
# This file drive ALL modules compilation
#
# things to remember
# - the declaration order is followed
# - the container must be a unique name, by convention we use mod000
# - if you don't want a project to compile, just comment it out
# 
#

logd.name = logd
logd.path = libraries

core.name = core
core.path = packages

eden.name = eden
eden.path = libraries

system_terminals.name = system_terminals
system_terminals.path = packages

astuce.name = astuce
astuce.path = libraries

system_signals.name = system_signals
system_signals.path = packages

